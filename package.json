{
  "scripts": {
    "dev:backend": "cd backend; yarn dev",
    "dev:frontend": "cd frontend; wait-on tcp:8800 && yarn dev",
    "dev": "npm-run-all -l -p dev:*"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "wait-on": "^7.0.1"
  }
}